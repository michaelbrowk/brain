/** PLACEHOLDER. Task 3 of the reminders-and-notifications plan replaces this
 *  file whole with the new-mail poll: the accounts it reads, the high-water
 *  mark it keeps and the `mail-new` rows it appends.
 *
 *  The timer in `lib/reminders/scheduler.ts` already calls it on every second
 *  tick, so the poll arrives without touching the scheduler again.
 */

export async function runMailScan(): Promise<void> {}
