/** The selection capsule is shared by every row in the sidebar panel — the
 *  page tree, the Mail row, the mail folders in the mail slot — so it flows
 *  between them on SPRING_SELECT (one framer `layoutId`). */
export const SIDEBAR_SELECT_LAYOUT_ID = "sidebar-selected";
