- The plus makes a task or a message as well as a page. It opens a menu now, holding Task, Message, and then the blank page and the templates it always did, because Brain keeps three kinds of thing and only one of them had a button. Task opens the Tasks column with the caret already in the capture row, Message opens Mail with a blank composer up, and the Message row is absent when there is no account to send from. The phone's plus opens the same menu as a sheet above the tab bar, and the palette lists all three from any surface.
- The bell and the plus in the sidebar head are one pair. The bell was 28 standing 4px from a 34 circle, so two controls that mean the same kind of thing were drawn a fifth apart in size and touching. Both are 34 with 12 of air between them, and the unread count sits in the bell's own corner instead of hanging out of it.
