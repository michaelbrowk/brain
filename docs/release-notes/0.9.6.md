- Images in an email now appear when you open it. Brain fetches a message's images for you rather than waiting for its background pass, so the reader no longer shows empty grey boxes over pictures that were already on their way.
- When an image cannot be shown at all, Brain stops asking for it instead of retrying in vain, and the log now records what happened to every image it fetched. That is how this bug was found.
