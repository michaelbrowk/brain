- The share popover sits on its glass. Its rows used to stand on a white plate inside the panel, so the popover read as a card on a card and the page behind it never showed through. The plate is gone: the rows lie on the same regular glass as every other popover, in both themes, with the hairlines and the switches where they were.
